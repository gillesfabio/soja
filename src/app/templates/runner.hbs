<div class="row">
	<div id="runner-info" class="col-md-12"></div>
</div>
<div class="row">
	<div class="col-md-12">
		<% if (features.length) { %>
		<div id="runner-tests">
			<div class="panel panel-default">
				<div class="panel-heading">
					<% if (runner) { %>
					<h3 class="panel-title"><%= runner.name %></h3>
					<% } %>
				</div>
				<div class="panel-body feature">
					<ul class="list-unstyled" id="runner-features">
						<% _.each(features, function(feature) { %>
						<li class="<%= feature.status === 'success' ? 'feature-success' : 'feature-failure' %>">
							<i class="icon-<%= feature.status === 'success' ? 'ok' : 'remove' %>"></i>  <%= feature.description %>
							<% if (feature.status === 'failure' && feature.reason) { %>
							<ul class="feature-reason-detail">
								<% if (feature.reason.title) { %><li><%= feature.reason.title %></li><% } %>
								<% if (feature.reason.help) { %><li><%= feature.reason.help.replace('<', '').replace('>', '') %></li><% } %>
							</ul>
							<% if (feature.reason.source) { %>
							<pre class="runner-failure-reason">
<%= feature.reason.source %>
							</pre>
							<% } %>
							<% } %>
						</li>
						<% }); %>
					</ul>
				</div>
			</div>
		</div>
		<% } %>
	</div>
</div>
